export DIRENV_WARN_TIMEOUT=60s

# GitHub identity: markus-barta (from macOS Keychain)
export GH_TOKEN=$(security find-generic-password -s "gh-token-markus-barta" -w 2>/dev/null)

eval "$(devenv direnvrc)"

use devenv
